string 这是谁(int 编号) {
    if (编号 == 1)
        return "顾小逸";
    else if (编号 == 2)
        return "赖睿航";
    else
        return "姚远";
}

void 开始() {
    for (int 编号 = 1; 编号 <= 3; 编号++)
        println(这是谁(编号));
}

int main() {
    开始();
    return 0;
}
